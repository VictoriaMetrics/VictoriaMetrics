export const getGroupsUrl = (server: string): string => {
  return `${server}/api/v1/rules`;
};

export const getNotifiersUrl = (server: string): string => {
  return `${server}/api/v1/notifiers`;
};
